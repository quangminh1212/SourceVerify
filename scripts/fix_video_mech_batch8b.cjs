/**
 * Fix TEMPLATE_MECH - Video methods batch 8b (temporal_frequency through watermark)
 */
const fs = require('fs');
const path = require('path');

const mechanisms = {
  'video/temporal_frequency_anomaly': `This method detects temporal frequency anomalies for video deepfake detection:\n\n1. **Temporal Signal Extraction**: Per-pixel or per-region temporal signals are extracted — tracking intensity, color, and texture over time. These signals capture the temporal dynamics at each spatial location.\n\n2. **Temporal Frequency Decomposition**: Temporal signals are analyzed via FFT to obtain per-location temporal frequency spectra. This reveals the frequency components of temporal variation: scene motion (~0-5 Hz), lighting changes (~0-1 Hz), and potential generation artifacts.\n\n3. **Anomaly Detection in Temporal Spectra**: Natural video temporal spectra are dominated by low frequencies (slow scene changes) with rapid falloff at higher frequencies. The method detects anomalous spectral components: (a) energy at incommensurate frequencies with no physical source, (b) spectral peaks at the video generator's characteristic temporal frequencies, (c) unexpected harmonics from repetitive generation patterns.\n\n4. **Spatial Mapping of Temporal Anomalies**: The detected temporal frequency anomalies are mapped spatially. Anomalies concentrated in face regions (but absent in background) strongly indicate face-specific generation artifacts. The spatial distribution of temporal anomalies reveals the extent and boundaries of generated content.\n\n5. **Cross-Frequency Coherence**: In natural video, temporal frequency components at different spatial frequencies are coherently related (same physical motion produces correlated temporal variations at all spatial frequencies). The method checks cross-frequency coherence — AI generation may produce temporally incoherent variations at different spatial scales.`,

  'video/temporal_gradient': `This method analyzes temporal gradient patterns for video deepfake detection:\n\n1. **Temporal Gradient Computation**: The frame-to-frame difference (temporal gradient) I_t(x,y) = I(x,y,t+1) − I(x,y,t) is computed for each pixel. This captures the instantaneous rate of change of image intensity over time.\n\n2. **Temporal Gradient Statistics**: The distribution of temporal gradient values is analyzed. Natural video shows specific gradient distributions: approximately Laplacian with width depending on motion speed. The method detects anomalous gradient distributions — too narrow (overly stable, suggesting generated smoothing) or too wide (excessive temporal variation).\n\n3. **Spatial-Temporal Gradient Relationship**: The relationship between spatial gradients (edges) and temporal gradients (motion) should follow the optical flow constraint equation: I_x·u + I_y·v + I_t = 0. The method checks this constraint. Generated video may violate the brightness constancy assumption in ways inconsistent with any physical motion.\n\n4. **Face-Background Gradient Comparison**: Temporal gradient statistics of face and background regions are compared. In authentic video from a single camera, both show similar temporal noise characteristics. Deepfakes may show different temporal gradient statistics in the face (from the generation process) compared to the background (from the camera).\n\n5. **Temporal Gradient Map Analysis**: A map of temporal gradient magnitude reveals which regions change most between frames. In authentic video, high-gradient regions correspond to moving objects and edges. Deepfake artifacts may produce elevated temporal gradients at face boundaries, blending zones, or throughout the generated face due to per-frame rendering inconsistency.`,

  'video/temporal_jitter': `This method detects temporal jitter for video deepfake detection:\n\n1. **Jitter Measurement**: Sub-pixel temporal jitter is measured across the video — the high-frequency component of point trajectories after removing the smooth (intentional) motion component. Jitter represents frame-to-frame positional noise.\n\n2. **Jitter Source Classification**: Different jitter sources produce different characteristics: (a) camera sensor noise → spatially uniform, small amplitude, (b) compression jitter → block-aligned, (c) deepfake generation jitter → content-dependent, potentially larger amplitude. The method classifies detected jitter by source.\n\n3. **Face-Background Jitter Comparison**: In authentic video, jitter characteristics are uniform across the frame (same camera). The method compares jitter in face regions vs. background. Deepfakes typically show elevated jitter in the generated face — the generator introduces positional uncertainty that differs from the camera's native noise.\n\n4. **Jitter Temporal Structure**: Camera jitter has flat temporal spectrum (white noise). Generation jitter may show characteristic temporal structure — periodicity from the generation pipeline, correlation patterns from autoregressive generation, or specific spectral color from the training process.\n\n5. **Jitter Amplitude Assessment**: The amplitude of detected jitter is compared against expected values for the estimated camera system. Excessive jitter (beyond what the camera would produce) in face regions indicates generation artifacts. The method quantifies excess jitter as a deepfake indicator.`,

  'video/temporal_noise': `This method analyzes temporal noise patterns for video deepfake detection:\n\n1. **Temporal Noise Extraction**: The temporal noise component is extracted by computing per-pixel temporal variance in static regions. This isolates noise from signal, capturing the stochastic temporal fluctuation characteristics of the imaging system.\n\n2. **Noise Model Identification**: Camera temporal noise follows specific models: (a) photon shot noise (Poisson, signal-dependent), (b) read noise (Gaussian, signal-independent), (c) thermal noise (dark current). The method fits these physical noise models to the observed temporal noise. AI-generated video typically doesn't follow physical camera noise models.\n\n3. **Noise Spatial Homogeneity**: Camera noise is spatially homogeneous (same sensor for all pixels) with possible variation from vignetting. The method checks noise spatial distribution — deepfakes show different noise characteristics in generated face regions (from the generator's stochastic process) vs. captured background (from camera noise).\n\n4. **Noise-Signal Dependency**: In camera-captured video, noise variance depends on signal level (shot noise ∝ signal). The method checks this dependency — AI-generated noise may be signal-independent (additive noise model) or show incorrect signal-noise relationship.\n\n5. **Temporal Noise Correlation**: Camera temporal noise is temporally uncorrelated (white). The method checks temporal autocorrelation of the noise signal — AI-generated temporal noise may show temporal correlations from the generation process (autoregressive generation, frame interpolation), producing non-white temporal noise distinguishable from camera noise.`,

  'video/texture_flow': `This method analyzes texture flow patterns in video for deepfake detection:\n\n1. **Texture Flow Estimation**: The motion field of texture elements (rather than edge features) is estimated using oriented texture correlation between frames. This captures how textured regions (skin, hair, fabric) move over time.\n\n2. **Texture-Motion Consistency**: Texture flow should be consistent with optical flow — texture moves with the surface it belongs to. The method compares texture flow with geometric optical flow (from edge features). Inconsistencies indicate texture that moves independently of the underlying geometry — a possible AI generation artifact.\n\n3. **Surface-Texture Coherence**: As surfaces rotate, textures foreshorten according to perspective. The method checks whether texture deformation is consistent with the estimated surface orientation change. Deepfake face generators may rotate faces without properly foreshortening skin texture.\n\n4. **Texture Temporal Stability**: Fixed textures (skin pores, wrinkles, fabric pattern) should maintain their spatial relationship to surface geometry across frames. The method tracks texture-surface binding — texture that \"slides\" relative to the surface (like a projection rather than a surface property) indicates AI rendering.\n\n5. **Texture Flow Boundaries**: Texture flow should be continuous within objects and show discontinuities only at object boundaries (depth edges, occlusion boundaries). The method detects texture flow discontinuities within surfaces — internal flow breaks corresponding to face-swap boundaries reveal the extent of generated regions.`,

  'video/tongue_consistency': `This method analyzes tongue rendering and consistency for deepfake detection:\n\n1. **Tongue Detection**: The tongue is detected when visible during speech and expressions (tongue protrusions, licking lips, certain phoneme articulations like /l/, /t/, /d/). Tongue shape, color, and surface texture are characterized per frame.\n\n2. **Tongue Anatomy Verification**: Natural tongues have specific anatomical features: dorsal surface texture (papillae), central groove (median sulcus), smooth ventral surface, characteristic pink-red coloration. The method checks anatomical correctness. AI-generated tongues may show: incorrect color, missing texture, wrong shape, or physically impossible configurations.\n\n3. **Tongue-Phoneme Coordination**: Tongue position should correspond to articulated phonemes — visible tongue tip for /l/, /th/, tongue retraction for /k/, /g/. The method checks whether tongue visibility and position correlate correctly with detected speech sounds. Deepfakes may show tongue positions inconsistent with the audio.\n\n4. **Temporal Tongue Stability**: When the tongue is not actively moving (resting in closed mouth, sustained position during speech holds), its appearance should remain stable between frames. The method tracks tongue consistency — AI generation may produce flickering tongue texture or shape changes during supposedly static tongue positions.\n\n5. **Tongue-Teeth-Lip Interaction**: During speech, tongue, teeth, and lips interact in coordinated, anatomically constrained ways. The method checks whether these interactions are physically plausible. Deepfakes may produce impossible interaction states — tongue passing through teeth or occupying impossible positions relative to the oral cavity.`,

  'video/video_artifact_grid': `This method detects grid-like artifacts for video deepfake detection:\n\n1. **Grid Pattern Detection**: The method searches for periodic grid-like artifacts in the spatial domain using autocorrelation and Fourier analysis. Grid artifacts appear as periodic intensity patterns at specific spatial frequencies — common artifacts from GAN generators using transposed convolution or pixel shuffle upsampling.\n\n2. **Transposed Convolution Artifact Identification**: When GANs use transposed convolution for upsampling, \"checkerboard artifacts\" appear at the stride frequency. The method specifically detects these stride-related grid patterns using frequency-domain analysis targeting the known artifact frequencies.\n\n3. **Block Grid Detection**: Video compression creates 8×8 or 16×16 block boundaries. The method distinguishes between legitimate compression block grids and generation artifact grids by analyzing grid frequency, grid phase, and grid-content relationship. Generation grids are typically at different frequencies than compression grids.\n\n4. **Spatial Grid Localization**: Detected grid artifacts are mapped spatially. Grid artifacts appearing only in face regions (not in background) strongly indicate face generation. The spatial extent of grid artifacts reveals the boundaries of generated content.\n\n5. **Temporal Grid Stability**: Compression block grids are temporally stable (fixed grid phase). Generation grids may be temporally unstable — shifting phase between frames as the generator produces each frame independently. The method distinguishes stable (compression) from unstable (generation) grids for forensic classification.`,

  'video/video_blockiness': `This method analyzes video blockiness artifacts for forensic detection:\n\n1. **Block Boundary Detection**: Block coding boundaries (typically 8×8 or 16×16 pixels for H.264/H.265) are detected by measuring intensity discontinuities at these regular intervals. The strength of block boundaries quantifies blockiness.\n\n2. **Blockiness Distribution Analysis**: The spatial distribution of blockiness across the frame is analyzed. Natural single-encoded video shows blockiness correlated with content complexity and QP allocation. Re-encoded video (after manipulation) may show double-blockiness patterns or blockiness distribution inconsistent with the current QP map.\n\n3. **Face-Background Blockiness Comparison**: Blockiness characteristics should be consistent across the frame from a single encoding pass. The method compares blockiness in face regions vs. background. Deepfakes compositing a separately encoded/generated face may show different blockiness characteristics between face and background.\n\n4. **Temporal Blockiness Pattern**: Blockiness varies naturally with frame type (I-frames show different blockiness than P/B-frames). The method analyzes temporal blockiness patterns — they should follow the GOP structure predictably. Anomalous temporal blockiness patterns indicate manipulation or re-encoding.\n\n5. **Block Misalignment Detection**: If video has been cropped, resized, or reframed (common in deepfake processing), the coding block grid may be misaligned with the original grid, creating \"grid shift\" detectable as double block boundaries at offset positions at the grid misalignment.`,

  'video/video_color_balance': `This method analyzes video color balance for forensic detection:\n\n1. **White Balance Estimation**: The illuminant color temperature is estimated per frame using white point detection, gray world assumption, and highlight analysis. The estimated white balance should be consistent across the frame and stable over time.\n\n2. **Face-Background White Balance Comparison**: The effective white balance of the face and background should match (same illumination). The method compares white point estimates from face and background. Deepfake face insertion may introduce a face rendered under different white balance — warmer or cooler color temperature than the environment.\n\n3. **Color Channel Balance**: The RGB channel balance (relative mean values) is tracked across frames. Natural video shows stable channel balance within scenes. Deepfake generation may produce subtle channel imbalances, particularly visible in neutral-colored face regions where white balance errors become apparent.\n\n4. **Color Cast Detection**: The method detects unwanted color casts (systematic color shift) in the face compared to the surrounding area. Even sophisticated deepfakes may impart subtle color casts from the training data or generator's learned bias, producing faces with slightly different color bias than the scene.\n\n5. **Temporal Color Balance Stability**: Color balance should vary slowly with lighting changes. The method tracks color balance stability — high-frequency color balance fluctuations in the face region (while background remains stable) indicate per-frame independent color rendering in the deepfake generation process.`,

  'video/video_denoising_trace': `This method detects denoising traces for video forensic analysis:\n\n1. **Noise Level Estimation**: The noise level and noise model of the video are estimated per frame. This establishes the baseline expected noise characteristics for the imaging pipeline.\n\n2. **Denoising Signature Detection**: Denoising algorithms leave characteristic signatures: (a) selective frequency attenuation (noise reduction in high frequencies while preserving edges), (b) stair-casing artifacts (from total variation denoising), (c) hallucinated details (from neural network denoising). The method identifies these signatures.\n\n3. **Selective Denoising Detection**: The method checks whether denoising has been selectively applied to specific regions. Authentic video is either entirely denoised or not at all. Selective denoising (face denoised but background not) indicates targeted processing — potentially part of a deepfake pipeline's post-processing.\n\n4. **Denoising-Resolution Consistency**: Denoising affects the effective resolution of the image (removing high-frequency noise also removes some high-frequency detail). The method checks whether denoised regions show consistent effective resolution with non-denoised regions. Resolution mismatches reveal processing different regions with different denoising parameters.\n\n5. **Multi-Pass Denoising Detection**: Multiple denoising passes produce cumulative signatures — progressive loss of fine detail. The method detects evidence of multiple denoising steps, consistent with a deepfake pipeline: generator output → denoising → blending → additional denoising to smooth blending artifacts.`,

  'video/video_frame_rate': `This method analyzes video frame rate characteristics for forensic detection:\n\n1. **Effective Frame Rate Estimation**: The actual effective frame rate is measured from inter-frame differences (beyond the declared container frame rate). This detects: (a) duplicated frames (effective rate lower than declared), (b) frame interpolation (artificially increased rate), (c) variable frame rate.\n\n2. **Frame Rate Conversion Detection**: If the deepfake was generated at a different frame rate and converted, frame rate conversion artifacts are detectable: blend frames (intermediate alpha-blended frames), motion trajectory irregularities, and periodic motion discontinuities at the source-target rate ratio.\n\n3. **Face-Background Frame Rate Consistency**: In composited deepfakes, the face and background may originate from different frame rate sources. The method independently estimates effective frame rates for face and background regions using their respective motion characteristics. Frame rate mismatches indicate compositing.\n\n4. **Temporal Aliasing Analysis**: Frame rate determines the temporal sampling frequency (Nyquist limit). Motion exceeding the Nyquist limit produces temporal aliasing (wagon wheel effect). The method checks whether aliasing behavior is consistent across the frame. Different effective frame rates in face vs. background produce different aliasing characteristics.\n\n5. **Frame Timing Regularity**: The inter-frame timing should be regular for constant-frame-rate video. The method analyzes timing regularity — irregular intervals may indicate: variable frame rate from the generator, dropped frames during processing, or asynchronous capture-generation timing.`,

  'video/video_freq_spectrum': `This method analyzes the video frequency spectrum for forensic detection:\n\n1. **2D Spatial Frequency Analysis**: Per-frame 2D FFT reveals the spatial frequency spectrum. The method analyzes the overall spectral shape, spectral peaks, and spectral symmetry. Natural images follow approximately 1/f spectral decay.\n\n2. **Spectral Anomaly Detection**: AI generators produce characteristic spectral anomalies: (a) GAN checkerboard peaks at upsampling stride frequencies, (b) diffusion model spectral gaps at certain frequency bands, (c) autoencoder spectral truncation at the bottleneck resolution. The method detects these architecture-specific spectral signatures.\n\n3. **Face-Background Spectral Comparison**: The spatial frequency spectrum of face regions and background should be consistent (same imaging pipeline). The method compares spectral shapes — generated faces may show different spectral characteristics (different noise spectrum, different high-frequency falloff, or additional spectral peaks from generation artifacts).\n\n4. **Temporal Spectral Stability**: The spatial frequency spectrum should be temporally stable for static content. The method tracks spectral stability — deepfake generation may produce temporally varying spectral content as the generator's output fluctuates frame-to-frame.\n\n5. **3D Spatio-Temporal Spectrum**: The combined spatial-temporal frequency spectrum (3D FFT over x, y, t) reveals motion structure. Natural motion produces characteristic spatio-temporal spectral planes. AI-generated motion may show different spectral structure — particularly at the temporal frequency boundaries of the generation model.`,

  'video/video_global_illum': `This method analyzes global illumination consistency for video deepfake detection:\n\n1. **Illumination Environment Estimation**: The scene's illumination environment is estimated per frame from multiple cues: shadow directions and intensities, specular highlights, ambient illumination level, and color temperature. This builds a per-frame lighting model.\n\n2. **Face-Scene Illumination Agreement**: The illumination on the face should be consistent with the estimated scene illumination. The method compares: (a) light direction on the face vs. scene shadows, (b) ambient-to-direct illumination ratio on face vs. scene, (c) color temperature of face illumination vs. scene. Deepfakes generated under different lighting won't match the scene.\n\n3. **Inter-Object Illumination Consistency**: All objects in the scene (including the face) should show consistent illumination from the same light sources. The method checks illumination consistency across multiple objects — if the face shows different lighting than a nearby object at similar distance from the light source, manipulation is indicated.\n\n4. **Global Illumination Effects**: Natural scenes show secondary illumination effects: color bleeding (colored surfaces tint nearby surfaces), ambient occlusion (corners and crevices are darker), and environmental reflections. The method checks whether these global illumination effects are present and consistent between the face and scene. AI generators often don't model these subtle inter-object light interactions.\n\n5. **Temporal Illumination Coherence**: Scene illumination should change smoothly and affect all objects simultaneously. The method tracks illumination parameters over time — face illumination should correlate with scene illumination changes. Independent illumination variation on the face indicates it was generated separately from the scene.`,

  'video/video_grain': `This method analyzes video grain (film grain / sensor noise) for forensic detection:\n\n1. **Grain Pattern Extraction**: The fine-grained noise/grain pattern is extracted by separating the high-frequency stochastic component from image structure using adaptive filtering. The grain's spatial and statistical characteristics are measured.\n\n2. **Grain Model Analysis**: The grain model (size, intensity, spatial frequency content, color characteristics) is identified. Camera sensor noise has specific properties: (a) Gaussian-Poisson distribution, (b) sensor-dependent spatial correlation, (c) color-dependent characteristics (more noise in blue channel). The method checks if the grain follows a physical sensor noise model.\n\n3. **Grain Spatial Homogeneity**: Camera grain should be spatially homogeneous (uniform sensor produces uniform noise). The method checks grain homogeneity across the frame — different grain characteristics in the face vs. background indicate compositing of content from different sources.\n\n4. **Grain Temporal Behavior**: Camera grain is temporally random (uncorrelated between frames) and should renew completely each frame. The method checks temporal grain independence. AI-generated grain may show temporal correlations — persistent grain patterns across frames or correlated grain changes.\n\n5. **Grain-Signal Interaction**: Real camera grain is multiplicative (signal-dependent) — brighter regions show more shot noise. The method checks grain-signal dependency. AI generators often add additive noise (signal-independent) or no noise at all, producing an unrealistic grain-signal relationship detectable as a forensic indicator.`,

  'video/video_hash': `This method uses perceptual video hashing for forensic analysis:\n\n1. **Perceptual Hash Computation**: A perceptual hash is computed per frame using established algorithms (pHash, dHash, wavelet hash). The hash captures the perceptual content summary — robust to minor modifications but sensitive to content changes.\n\n2. **Temporal Hash Stability**: Frame-to-frame hash changes are tracked. Natural video shows smooth hash evolution. The method detects anomalous hash transitions — sudden hash changes without corresponding visual scene changes, or hash stability during what should be content changes (freeze-frame manipulation).\n\n3. **Known Content Matching**: Frame hashes are optionally compared against databases of known deepfake content or original source material. Hash matching can identify specific deepfake videos or trace the source footage used in a face swap.\n\n4. **Hash-Based Tampering Detection**: The ratio of frame-to-frame hash distance to visual change magnitude is analyzed. Manipulated frames may show hash distances disproportionate to the apparent visual change — indicating content modification below the perceptual threshold but detectable in the hash space.\n\n5. **Multi-Resolution Hashing**: Hashes computed at multiple resolutions capture changes at different spatial scales. Face-swap deepfakes may produce normal hashes at coarse resolution (overall structure preserved) but anomalous hashes at fine resolution (detail-level generation artifacts), creating a resolution-dependent hash signature.`,

  'video/video_luma_range': `This method analyzes video luminance range for forensic detection:\n\n1. **Dynamic Range Analysis**: The luminance range (minimum to maximum luma values) is measured per frame. The method checks whether the content uses the expected luma range for the video standard (broadcast range 16-235 for BT.601/709, full range 0-255 for full-range content).\n\n2. **Range Consistency Check**: The luma range should be consistent across the frame and over time. The method detects regions with different effective dynamic ranges — composited deepfake faces may show different dynamic range (from the generator's output mapping) than the original background footage.\n\n3. **Highlight/Shadow Clipping Analysis**: The behavior of highlights (near maximum luma) and shadows (near minimum luma) is analyzed. Natural camera capture produces characteristic clipping characteristics. AI generators may produce different highlight/shadow rendering — either clean clipping (missing the natural rolloff) or extended range (values the capture format shouldn't contain).\n\n4. **Temporal Range Evolution**: Dynamic range typically remains stable within a scene. The method tracks luma range statistics over time — sudden range changes without corresponding scene changes indicate processing artifacts.\n\n5. **Gamma/Transfer Function Analysis**: The effective transfer function (gamma curve) is estimated from the luma distribution. Different cameras and generators use different transfer functions. Mismatched transfer functions between face and background reveal content from different imaging pipelines.`,

  'video/video_noise': `This method analyzes video noise consistency for forensic detection:\n\n1. **Noise Estimation**: Per-region noise level and noise characteristics are estimated from each frame using smooth-region analysis and noise estimation algorithms. The noise model (Gaussian, Poisson, mixed) and parameters (variance, spatial correlation) are determined.\n\n2. **Noise Spatial Consistency**: Camera noise should be spatially consistent — same sensor, same noise for all regions at a given signal level. The method compares noise characteristics between face and background. Deepfake composites show noise mismatches — the generated face has different noise (from the generator) than the captured background (from the camera sensor).\n\n3. **Noise-Signal Relationship**: Camera noise follows specific noise-signal relationships: photon shot noise creates variance proportional to signal. The method checks this relationship across the frame. AI-generated content may show incorrect noise-signal dependencies — either constant noise (additive model) or noise patterns that don't follow physical sensor models.\n\n4. **Noise Spectral Shape**: Camera sensor noise has specific spatial frequency characteristics determined by the anti-aliasing filter and readout electronics. The method analyzes noise power spectral density — AI generators produce noise with different spectral characteristics than camera sensors.\n\n5. **Temporal Noise Analysis**: Frame-to-frame noise should be independently distributed (uncorrelated). The method checks temporal noise independence — AI-generated frames may show correlated noise patterns (repeated noise structures between frames or systematically evolving noise) inconsistent with camera capture.`,

  'video/video_resolution_map': `This method creates resolution maps for video deepfake detection:\n\n1. **Local Resolution Estimation**: Effective spatial resolution is estimated locally using edge sharpness analysis, modulation transfer function (MTF) estimation, and texture detail measurements. This creates a per-region resolution map revealing where the image is effectively sharp vs. degraded.\n\n2. **Resolution Consistency Check**: Resolution across the frame should follow the camera's optical characteristics: uniform within the depth of field, decreasing with distance from the focal plane, potentially soft at frame corners (lens vignetting). The method checks for consistency. Deepfakes may show a face with different effective resolution than the background.\n\n3. **Resolution Boundary Detection**: The method detects boundaries between regions of different effective resolution. Abrupt resolution changes not corresponding to depth boundaries indicate compositing — where a generated face (at the generator's output resolution) meets original footage (at the camera's capture resolution).\n\n4. **Upsampling/Downsampling Detection**: The method detects evidence of resolution changes — interpolation artifacts from upsampling (ringing, aliasing) or detail loss from downsampling. These artifacts indicate the content has been resized, consistent with deepfake pipelines that generate at one resolution and resize to match the target video.\n\n5. **Temporal Resolution Stability**: Effective resolution should remain stable across frames for static content. The method tracks resolution maps over time — deepfake generation may produce temporally varying effective resolution as the generator's output detail fluctuates frame-to-frame.`,

  'video/video_saturation': `This method analyzes video saturation for forensic detection:\n\n1. **Saturation Measurement**: Color saturation is measured per frame in HSV and Lab color spaces. Per-region saturation statistics (mean, variance, distribution shape) capture the color vibrancy characteristics.\n\n2. **Face-Background Saturation Comparison**: Saturation levels of the face and background should be consistent (same camera, same color grading). The method compares saturation statistics — deepfake faces may show different saturation characteristics from the generation model's output color space mapping.\n\n3. **Skin Tone Saturation**: Human skin follows specific saturation ranges dependent on skin type (Fitzpatrick scale). The method checks whether face skin saturation falls within plausible physiological ranges. AI generators may produce oversaturated or undersaturated skin inconsistent with natural skin tones.\n\n4. **Temporal Saturation Stability**: Saturation should vary smoothly over time, consistent with illumination changes. The method tracks saturation stability — high-frequency saturation fluctuations in the face (while background remains stable) indicate per-frame rendering inconsistency from deepfake generation.\n\n5. **Saturation-Luminance Relationship**: In natural images, saturation and luminance show specific relationships (Hunt effect — saturation appears higher at higher luminance). The method checks whether the saturation-luminance relationship follows natural perception models. AI generators may produce content with incorrect saturation-luminance coupling.`,

  'video/video_saturation_map': `This method creates detailed saturation maps for video deepfake detection:\n\n1. **Spatial Saturation Mapping**: A per-pixel saturation map is created per frame, revealing the spatial distribution of color saturation across the image. The map highlights regions of high and low saturation.\n\n2. **Saturation Boundary Detection**: The method detects boundaries between regions of different saturation characteristics. Natural images show smooth saturation transitions. Deepfake composites may show saturation discontinuities at face-background boundaries where the generated face has different color rendering.\n\n3. **Saturation Anomaly Detection**: The method identifies regions with anomalous saturation — either oversaturated (common in GAN-generated content) or undersaturated (potential desaturation processing). Per-region anomaly scoring highlights suspicious areas.\n\n4. **Multi-Scale Saturation Analysis**: Saturation is analyzed at multiple scales. Natural images show consistent saturation characteristics at all scales. AI generation may produce scale-dependent saturation anomalies — correct at coarse scales but incorrect at fine scales where the generator struggles to reproduce color detail.\n\n5. **Temporal Saturation Map Evolution**: The saturation map is tracked across frames. Regions with temporally unstable saturation — flickering between saturated and desaturated states — indicate generation artifacts. The temporal stability of the saturation map provides an additional forensic dimension.`,

  'video/video_sharpness': `This method analyzes video sharpness for forensic detection:\n\n1. **Per-Frame Sharpness Measurement**: Global and local sharpness metrics (edge acutance, MTF50, high-frequency energy ratio) are computed per frame. This quantifies the optical sharpness of the video content.\n\n2. **Sharpness Spatial Distribution**: Sharpness variation across the frame should follow optical physics: (a) sharp at the focal plane, (b) decreasing with defocus, (c) potentially reduced at frame corners from lens aberrations. The method checks for physically motivated sharpness distribution. AI-generated content may show unnatural sharpness patterns.\n\n3. **Face-Background Sharpness Match**: The sharpness characteristics of face and background should be consistent with the same optical system and focus distance. The method detects sharpness mismatches — generated faces that are sharper or softer than expected for their position in the scene's depth of field.\n\n4. **Temporal Sharpness Consistency**: Sharpness should vary smoothly over time (focus changes are gradual). The method tracks frame-to-frame sharpness — rapid, unphysical sharpness changes indicate generation artifacts (variable output quality from the generator).\n\n5. **Sharpness Edge Profile Analysis**: The sub-pixel edge profile at sharp boundaries characterizes the imaging system's optical transfer function. The method compares edge profiles between face and background — different edge profiles indicate content from different optical or rendering pipelines.`,

  'video/video_spectral_coherence': `This method analyzes spectral coherence across video frame regions for deepfake detection:\n\n1. **Regional Spectral Analysis**: The spatial frequency spectrum is computed independently for multiple regions: face, background, clothing, hair. Each region's spectral characteristic — spectral slope, cutoff frequency, spectral peaks — is measured.\n\n2. **Cross-Region Spectral Coherence**: All regions captured through the same optical system should show coherent spectral characteristics — same noise spectrum, same spectral cutoff (determined by the lens MTF), same compression artifacts. The method checks cross-region spectral coherence. Deepfake composites show spectral incoherence between generated face and captured background.\n\n3. **Spectral Noise Floor Matching**: The spectral noise floor (high-frequency baseline noise level) should be uniform across the frame. The method compares noise floors between regions — different noise floors indicate content from different imaging/generation pipelines.\n\n4. **Spectral Transition Analysis**: At region boundaries (face-background border), the spectral transition should be smooth. The method analyzes spectral characteristics across boundaries — abrupt spectral changes at the face border (changing noise spectrum, changing spectral slope) indicate compositing.\n\n5. **Temporal Spectral Coherence**: The spectral relationship between regions should remain stable over time. The method tracks inter-region spectral ratios — temporal variation in the face-to-background spectral ratio indicates the face content varies independently from the background, consistent with deepfake generation.`,

  'video/voice_f0_analysis': `This method analyzes voice fundamental frequency (F0) for deepfake detection:\n\n1. **F0 Extraction**: The fundamental frequency (pitch) of the voice is extracted per frame using autocorrelation, cepstral, or YIN algorithm. F0 corresponds to the vocal fold vibration rate, typically 85-180 Hz for males and 165-255 Hz for females.\n\n2. **F0 Contour Analysis**: The temporal F0 contour (pitch track) is analyzed for naturalness. Natural speech shows characteristic F0 patterns: (a) declination (gradual F0 decrease across utterances), (b) prosodic contours (rises for questions, falls for statements), (c) micro-prosody (F0 perturbations at phoneme boundaries). AI voice synthesis may produce F0 contours with incorrect prosodic patterns or missing micro-prosodic detail.\n\n3. **F0 Range and Variability**: Each speaker has a characteristic F0 range and variability. The method checks whether F0 statistics (mean, standard deviation, range) fall within physiologically plausible values for the apparent speaker. AI-generated voices may show either unnaturally constrained (monotone) or excessive F0 variability.\n\n4. **Jitter and Shimmer**: Natural voice shows slight cycle-to-cycle F0 variation (jitter, typically <1%) and amplitude variation (shimmer, typically <3%). These micro-perturbations reflect the biomechanics of vocal fold vibration. AI voice synthesis may produce either too-perfect (zero jitter/shimmer) or incorrectly modeled perturbation patterns.\n\n5. **F0-Visual Correlation**: Voice F0 should correlate with visible physical effort — higher pitch may correspond with increased tension visible in the neck muscles. The method checks audio-visual F0 correspondence. Deepfakes combining generated audio with generated video may show poor F0-visual correlation.`,

  'video/watermark': `This method detects digital watermarks for video forensic authentication:\n\n1. **Visible Watermark Detection**: The method searches for visible watermarks — logos, text overlays, station identification marks. Their presence, position, and integrity provide provenance information. Cropping or overlaying to remove watermarks indicates potential manipulation intent.\n\n2. **Invisible Watermark Extraction**: Robust invisible watermarks embedded in the frequency domain (DCT, DWT coefficients) or spatial domain are extracted. Common watermarking standards (Digimarc, BWARN) are tested. Intact watermarks verify content authenticity; corrupted watermarks indicate post-watermark manipulation.\n\n3. **C2PA/Content Credentials Detection**: Modern content authenticity standards (Coalition for Content Provenance and Authenticity) embed cryptographically signed provenance metadata. The method checks for C2PA manifests and verifies digital signatures, providing chain-of-custody information for the content.\n\n4. **Watermark Consistency Analysis**: If watermarks are present, they should be consistent across all frames. The method checks temporal watermark stability — partial watermark removal (only in the deepfaked face region) or watermark corruption in specific frames indicates selective manipulation.\n\n5. **AI-Generated Content Watermark**: Some AI generators embed their own watermarks (SynthID by Google, DALL-E metadata). The method searches for known AI generator watermarks. Detection of an AI generator watermark provides direct evidence of synthetic content generation.`
};

let fixed = 0;
for (const [methodPath, newMechanism] of Object.entries(mechanisms)) {
  const enPath = path.join('src/app/methods', methodPath, 'i18n', 'en.json');
  try {
    const data = JSON.parse(fs.readFileSync(enPath, 'utf8'));
    data.mechanism = newMechanism;
    fs.writeFileSync(enPath, JSON.stringify(data, null, 2), 'utf8');
    fixed++;
    console.log(`✓ ${methodPath}`);
  } catch (err) {
    console.log(`✗ ${methodPath}: ${err.message}`);
  }
}
console.log(`\nFixed: ${fixed}/${Object.keys(mechanisms).length}`);
